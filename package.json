{
  "name": "mer-moj-novac",
  "version": "1.0.0",
  "private": true,
  "description": "MER savings, budgeting and cash-flow workspace",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "node scripts/preflight.js",
    "test": "node scripts/run-tests.js",
    "test:cycle1": "node scripts/run-tests.js cycle-1",
    "test:cycle2": "node scripts/run-tests.js cycle-2",
    "build": "node scripts/build.js",
    "check": "npm run lint && npm test && npm run build"
  },
  "devDependencies": {
    "terser": "5.50.0"
  }
}
